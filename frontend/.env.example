# 대시보드 → 백엔드 프록시 (서버 전용 — 브라우저로 안 나감)
BACKEND_URL=https://us-trading-production.up.railway.app
BACKEND_TOKEN=            # 백엔드 API_AUTH_TOKEN과 동일 값. 없으면 화면이 "백엔드 미연결"
# 라이브 WS (공개 브로드캐스트, 토큰 없음). 비우면 위 Railway 주소 기본값
NEXT_PUBLIC_BACKEND_WS_URL=
